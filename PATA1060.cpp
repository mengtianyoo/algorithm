#include<iostream>
#include<string>
using namespace std;
string process(string s, int &e, int n){
	if(s.find('.') != s.begin() + 1){
		while(s[s.begin()]) s.erase(s.begin());
	}
	
	return ;
}
int main(){
	int e, n;
	string n1, n2;
	cin >> e >> n1 >> n2;

	return 0;
}
