#include <iostream>
#include<math.h>
#inlucde<string>
using namespace std;

string so[100001], file[100001];

int main() {
	string str;
	getline(cin, str);
	for(i = 0; i < str.length(); i++){
		if(str[i] == '.'){
			cout<<s[]
		}
	}
	cout << str;
}
